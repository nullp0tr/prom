AC_INIT(src/main.c)

PACKAGE=prom
VERSION=0.0.1

AM_INIT_AUTOMAKE($PACKAGE, $VERSION)

PKG_CHECK_MODULES(CMOCKA, cmocka)
PKG_CHECK_MODULES(VTERM, vterm)
AC_SUBST(CMOCKA_CFLAGS)
AC_SUBST(CMOCKA_LIBS)
AC_SUBST(VTERM_CFLAGS)
AC_SUBST(VTERM_LIBS)

AC_PROG_CC

AC_OUTPUT(Makefile src/Makefile)
